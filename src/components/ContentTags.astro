---
import { Badge } from "./ui/badge";
const {tags} = Astro.props;
const base = import.meta.env.BASE_URL; // /bento/
---


<div class="flex flex-0 gap-1 flex-wrap">
  {tags.map((i) => (
    <a href={`${base}/tags/${i}`}>
      <Badge className="truncate">{i}</Badge>
    </a>
  ))}
</div>
